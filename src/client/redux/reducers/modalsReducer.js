import {
} from "../actions/types";

export const initialState = {
	tickerNew: false,
	proxyNew: false,
};

export const modalsReducer = (state = initialState, action) => {
	switch (action.type) {
		default:
			return state;
	}
};
